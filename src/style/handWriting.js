import handwriting from './handwriting.ttf'

const handWriting = `
@font-face {
    font-family: 'handWriting';
    src: url(${handwriting});
    font-weight: normal;
    font-style: normal;

}`

export default handWriting
